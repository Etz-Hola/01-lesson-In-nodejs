const math = require("./server");

const { subtract, multiply, divide } = require("./server");
console.log(math.add(2, 3));
console.log(math.subtract(2, 3));
console.log(math.multiply(2, 3));
console.log(math.divide(2, 3));